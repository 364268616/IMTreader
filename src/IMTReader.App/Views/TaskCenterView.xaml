<UserControl x:Class="IMTReader.App.Views.TaskCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource WindowBackgroundBrush}"
             FontFamily="{StaticResource UiFontFamily}">
    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                <TextBlock Text="任务中心" Style="{StaticResource PageTitle}" />
                <TextBlock Style="{StaticResource Hint}" Margin="0,5,0,0"
                           Text="识别任务在后台按顺序执行，期间可以继续阅读其它文献。" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <TextBlock x:Name="EngineText" VerticalAlignment="Center" Margin="0,0,14,0" Style="{StaticResource Hint}" />
                <Button Style="{StaticResource OutlineButton}" Content="取消选中任务" Click="Cancel_Click" Margin="0,0,8,0" />
                <Button Style="{StaticResource OutlineButton}" Content="清除已完成" Click="ClearFinished_Click" />
            </StackPanel>
        </Grid>

        <DataGrid x:Name="Grid" Grid.Row="1" Style="{StaticResource ListGrid}" ItemsSource="{Binding}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="文献" Binding="{Binding DocumentTitle}" Width="2*" MinWidth="130" />
                <DataGridTextColumn Header="任务" Binding="{Binding Title}" Width="2*" MinWidth="150" />
                <DataGridTextColumn Header="模式" Binding="{Binding Mode}" Width="80" />
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="130" />
                <DataGridTemplateColumn Header="进度" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ProgressBar Minimum="0" Maximum="1" Value="{Binding Progress, Mode=OneWay}" Height="5" Margin="0,0,10,0" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt}" Width="110" />
                <DataGridTextColumn Header="耗时" Binding="{Binding Elapsed}" Width="70" />
                <DataGridTextColumn Header="错误" Binding="{Binding Error}" Width="2*" MinWidth="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
