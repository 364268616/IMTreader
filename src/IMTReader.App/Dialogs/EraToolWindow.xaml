<Window x:Class="IMTReader.App.Dialogs.EraToolWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="纪年换算" Width="560" Height="460" MinWidth="420" MinHeight="320"
        WindowStartupLocation="CenterOwner" Background="{StaticResource ToolbarBrush}" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <Button Style="{StaticResource PrimaryButton}" Content="换算" Click="Convert_Click" DockPanel.Dock="Right" Margin="6,0,0,0" MinWidth="70" />
            <TextBox x:Name="InputBox" Style="{StaticResource FormTextBox}" FontSize="15" KeyDown="InputBox_KeyDown" />
        </DockPanel>
        <TextBlock Grid.Row="1" Style="{StaticResource Hint}" Margin="0,6,0,10"
                   Text="可输入：年号纪年（光绪二十年、光緒甲午、民国卅八年、清乾隆六十年）、公元年份（1894）或干支（甲午）。内置宋、元、明、清、民国年号表，其它朝代可用数据目录下的 eras.user.txt 扩展。" />
        <TextBox Grid.Row="2" x:Name="OutputBox" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 FontFamily="{DynamicResource ReadingFontFamily}" FontSize="15" Padding="10"
                 Background="{DynamicResource ReadingBackgroundBrush}" Foreground="{DynamicResource ReadingForegroundBrush}" />
    </Grid>
</Window>
